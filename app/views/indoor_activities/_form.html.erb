<%= form_for(@indoor_activity) do |f| %>
  <% if @indoor_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@indoor_activity.errors.count, "error") %> prohibited this indoor_activity from being saved:</h2>

      <ul>
      <% @indoor_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= collection_select(:category_id, 0, Category.all, :id, :name) %>
  </div>

  <div class="field">
    <%= collection_select(:audience_id, 0, Audience.all, :id, :name) %>
  </div>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :release_date %><br>
    <%= f.date_select :release_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
